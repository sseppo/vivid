# `docs/brainstorm/` — brainstorming material, kept as it was

**Nothing in this folder is edited, renamed or tidied by any agent but the one that produced it.** Filenames are kept
exactly, including typos and spaces, because other files cite them. The material may be messy and self-contradicting;
that is expected. It is **not** a statement of what was decided — decisions are in `docs/ledger/decisions.md`, and the
current product inputs are in `docs/context/sources/`.

The `.md` files here carry no status header, by design. The folder holds pre-repository material from two source
folders, under source labels (ADR 0008):

| subfolder | from |
|---|---|
| `api-visualisatium/` | `C:\api\visualisatium\` — NotebookLM sources and prompt drafts |
| `webapp-visualisatium/` | `C:\ai-stuff\Projects\WebApp\Active\Visualisatium\` — planning chats and ideas |

**One file is not readable as stored:** `api-visualisatium/prompts/muistiin'.txt` lists open technical questions that
nothing else in the corpus answers. A readable copy, with the questions marked unanswered, is outside this folder at
`docs/architecture/open-questions-muistiin.md` (R-38). The original stays here untouched.

Written 2026-09-19, Librarian session 7, from the pack's rule for this folder and plan §Plan.
